refactor parsing for polars (removing indexes/keeping timestamp columns, removing some pandas-specific methods)

generic functions accepting callables as arguments for "mode="

Cleanup ingestion, write readme and add a parse/align for funding rate

Add parser ohlcv data sampling for different bar resmplings (1m, 2m, 5m, ..., 1h, 1d, etc.)

Add proper custom pathing for outputs of logs and backtests and improce live logs

empirically measure market impact curve
